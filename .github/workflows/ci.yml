name: CI

on:
    pull_request:
    push:
        branches:
            - master
    workflow_dispatch:

jobs:
    validation:
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                php-version: [
                    '8.3',
                    '8.5'
                ]

        steps:
            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: ${{ matrix.php-version }}
                  extensions: mbstring, intl, pdo_mysql, grpc

            - name: Checkout
              uses: actions/checkout@v4

            - name: Composer validate
              run: composer validate

            - name: Composer get cache directory
              id: composer-cache
              run: |
                  echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

            - name: Composer cache
              uses: actions/cache@v4
              with:
                  path: ${{ steps.composer-cache.outputs.dir }}
                  key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
                  restore-keys: |
                      ${{ runner.os }}-composer-

            - name: Allow php-http/discovery Composer plugin
              # Global config only - keeps composer.json free of an allow-plugins entry
              # that dev:composer:update-json-files (in the suite monorepo) would strip
              # back out, since it enforces a single fixed allow-plugins entry there.
              run: composer global config --no-plugins allow-plugins.php-http/discovery true

            - name: Composer install
              run: |
                  composer --version
                  composer install --optimize-autoloader

            - name: Codeception tests
              run: |
                  if [[ ${{ matrix.php-version }} == '8.2' ]]; then
                    vendor/bin/codecept build
                    XDEBUG_MODE=coverage vendor/bin/codecept run --coverage-xml
                  else
                    composer test
                  fi

            - name: Code Coverage Report
              if: success() && matrix.php-version == '8.2'
              uses: codecov/codecov-action@v1

    prefer-lowest:
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                php-version: [
                    '8.3'
                ]

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: ${{ matrix.php-version }}
                  extensions: mbstring, intl, pdo_mysql, grpc

            - name: Get Composer Cache Directory
              id: composer-cache
              run: |
                  echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

            - name: Composer cache
              uses: actions/cache@v4
              with:
                  path: ${{ steps.composer-cache.outputs.dir }}
                  key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
                  restore-keys: ${{ runner.os }}-composer-

            - name: Allow php-http/discovery Composer plugin
              # Global config only - keeps composer.json free of an allow-plugins entry
              # that dev:composer:update-json-files (in the suite monorepo) would strip
              # back out, since it enforces a single fixed allow-plugins entry there.
              run: composer global config --no-plugins allow-plugins.php-http/discovery true

            - name: Composer prefer-lowest
              run: composer update --prefer-dist --no-interaction --prefer-lowest --prefer-stable -o

            - name: Codecept run test
              run: composer test
